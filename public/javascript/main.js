// Save filter options as variable
var selectedMood;
var selectedRating;

function setMood(selection) {
    selectedMood = selection.value;
}

function setRating(selection) {
    selectedRating = selection.value;
}